export const addInputs = (output) => {
    return {
        type: "ADDINPUTS",
        output: output 
    }
}

export const subtractInputs = (output) => {
    return {
        type: "SUBTRACTINPUTS",
        output: output 
    }
}

